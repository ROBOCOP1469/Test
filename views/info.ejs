<!DOCTYPE html>
<html>
<head>
    <title>Stock Management - Info</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            max-width: 600px;
            margin-top: 50px;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .alert-message {
            font-size: 1.2rem;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center">Information</h1>
        <hr>
        <p class="text-center">Logged in as <b><%= user.username %></b></p>
        <div class="alert alert-info alert-message text-center">
            <%= message %>
        </div>
        <a href="/" class="btn btn-secondary btn-block">Home</a>
    </div>
</body>
</html>